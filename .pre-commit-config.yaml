# === pre-commit-config.yaml ===========================================================================================
#                                               Sen Infrastructure
#                   Licensed under the Apache License v2.0 (SPDX-License-Identifier Apache-2.0).
#                                    See the LICENSE.txt file for more information.
#                   © Airbus SAS, Airbus Helicopters, and Airbus Defence and Space SAU/GmbH/SAS.
# ======================================================================================================================
#
# To use:
#
#     pre-commit install  # (runs every time you commit in git)
# Or:
#     pre-commit run -a
#
# To update this file:
#     pre-commit autoupdate
#

# install every stage with a single `pre-commit install`
#
# An existing .git/hooks/pre-push is moved to pre-push.legacy and still runs, first, with
# its exit code folded in. Why pre-push is here at all: see .github/scripts/precheck.sh.
default_install_hook_types: [pre-commit, commit-msg, pre-push]

repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v6.0.0
  hooks:
    - id: trailing-whitespace
    - id: end-of-file-fixer
    - id: detect-private-key
    - id: check-json
      # devcontainer.json is JSONC (comments allowed by its spec)
      exclude: ^\.devcontainer/
    - id: check-yaml
      # mkdocs.yml uses mkdocs-material python-name tags
      exclude: ^mkdocs\.yml$
    - id: check-added-large-files
    - id: check-merge-conflict
    - id: check-case-conflict
    - id: check-illegal-windows-names
    - id: check-toml
    - id: check-xml
      exclude: libs/gen/test/data/plantuml/error_format/error_format.xml # the FOM in error_format is broken on purpose,
                                                                         # so the parser has something to reject

# The clang-format major tracks the toolchain (LLVM_VERSION in
# tools/ci/Dockerfile); do not bump it independently.
- repo: https://github.com/pre-commit/mirrors-clang-format
  rev: v20.1.8
  hooks:
    - id: clang-format
      'types_or': [c++, c, cuda, proto, textproto]

- repo: https://github.com/cheshirekow/cmake-format-precommit
  rev: v0.6.13
  hooks:
    - id: cmake-format

- repo: https://github.com/jackdewinter/pymarkdown
  rev: v0.9.39
  hooks:
    - id: pymarkdown
      name: Checking markdown files
      # scan, not fix: fix mode exits successfully when it has nothing it can
      # fix, so violations it cannot repair went unreported, and the repairs it
      # does make have moved code out of list items and content tabs.
      args: [--config, .pymarkdown.yaml, scan]

- repo: https://github.com/astral-sh/ruff-pre-commit.git
  rev: v0.16.3
  hooks:
    - id: ruff-check
      args: [--fix]
    - id: ruff-format

- repo: https://github.com/gitleaks/gitleaks
  rev: v8.30.1
  hooks:
    - id: gitleaks

- repo: https://github.com/rhysd/actionlint
  rev: v1.7.12
  hooks:
    - id: actionlint

- repo: https://github.com/hadolint/hadolint
  rev: v2.15.1
  hooks:
    - id: hadolint-docker
      # Guards the CI image; example Dockerfiles are tutorial content
      files: ^tools/

# Covers every tracked Python file except three, each excluded for a reason
# rather than to hide a finding:
#   .cmake-format.py            a cmake-format config, not source: its names are
#                               injected by the loader, which is why ruff already
#                               carries noqa: F821 for it
#   .conan/test_packages/...    a second conanfile, and mypy resolves both to the
#                               module "conanfile"
#   apps/cli_gen/test/test20/   a second test.py, same collision
# --explicit-package-bases is what lets the rest resolve from the repository root.
- repo: https://github.com/pre-commit/mirrors-mypy
  rev: v2.3.1
  hooks:
    - id: mypy
      args: [--explicit-package-bases, --ignore-missing-imports]
      exclude: ^(\.cmake-format\.py|\.conan/test_packages/|apps/cli_gen/test/test20/)
      additional_dependencies: [types-PyYAML==6.0.12.20260815]

- repo: https://github.com/jorisroovers/gitlint
  rev: v0.19.1
  hooks:
    - id: gitlint

- repo: local
  hooks:
    # Everything "Python checks" runs, before the push instead of after it. A hook cannot
    # know the pull request title, so pass it by hand:
    #     .github/scripts/precheck.sh "the pull request title"
    - id: precheck
      name: The checks main.yaml runs, before pushing
      language: script
      entry: .github/scripts/precheck.sh
      stages: [pre-push]
      pass_filenames: false
      always_run: true

    - id: repository-script-tests
      name: Repository script tests
      language: python
      additional_dependencies: [pytest==9.1.1, pyyaml==6.0.3]
      entry: python -m pytest .github/scripts libs/kernel/test/integration -q
      # The workflows, because a lane definition is compared against the job carrying it,
      # and the integration drivers, which are plain python.
      files: ^(\.github/(scripts|workflows)/|libs/kernel/test/integration/)
      pass_filenames: false

    # A `docs/` path written in a comment, a config or a script is invisible to
    # mkdocs, so that whole class of reference rots silently. One did.
    - id: docs-references
      name: Documentation references resolve
      language: python
      entry: python .github/scripts/check_docs_references.py
      pass_filenames: false
      always_run: true

# Global Configuration
exclude: (examples/packages/hla_fom/.*|test/util/dummy_entities/hla/.*|.*.svg|.*.excalidraw|stl.tmLanguage.json)
